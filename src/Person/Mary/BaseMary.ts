module Person.Mary {
    export class BaseMary extends egret.Sprite {
        public constructor() {
            super()

            this.initControl()
        }

        /**
         * 可重写，用于按键绑定
         */
        private initControl():void {
            
        }

        private touchBegin(event:egret.Event):void {
            
        }
    }
}